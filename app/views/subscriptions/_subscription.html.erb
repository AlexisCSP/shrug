<% if current_user.id != user.id %>
  <div class="card">
    <div class="card-block">
      <div class="row">
        <div class="col-md-11">
          <p class="card-text">
            <span class="text-muted">
              <b><%= user.name %></b>
            </span><br>
          </p>
        </div>
      </div>
    </div>
  </div>
<% end%>
