<li>
  <%= link_to_if is_chat_room_in_radius?(chat_room), "Enter #{chat_room.title}", chat_room_path(chat_room) %>
  <% if current_user.admin? %>
  | <%= link_to "delete", chat_room, method: :delete,
                                data: { confirm: "Are you sure you want to delete the user?" } %>
  <% end %>
</li>
