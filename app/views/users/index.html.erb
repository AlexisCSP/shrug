<% provide(:title, 'All users') %>

<div id=userspage>
<h1>All users</h1>

<div class="paginate">
    <%= will_paginate @users, :users => false %>
   <!--<%= will_paginate @posts, :page_links => false %> -->
 </div>

<ul class="users">
  <%= render @users %>
</ul>


<div class="paginate">
   <%= will_paginate @users, :users => false %>
 </div>

</div>
<%= will_paginate %>
